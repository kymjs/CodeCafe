<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="top.codecafe"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <application
        android:name=".AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="top.codecafe.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.BlogDetailActivity"
            android:theme="@style/base_KJAppTheme"/>
        <activity
            android:name=".activity.XituDetailActivity"
            android:theme="@style/base_KJAppTheme"/>
        <activity
            android:name=".activity.MobelBrowserActivity"
            android:theme="@style/base_KJAppTheme"/>
        <activity
            android:name=".activity.BrowserActivity"
            android:theme="@style/base_KJAppTheme"/>
    </application>

</manifest>
